{{#each games as |game|}}

  <!-- <ul> -->
  <h1 data-id="{{game.id}}" class="my-game"> <small>{{game.title}}</small></h1>
  <ul data-id="{{game.id}}">
    ID: {{#if game.id}}
      {{game.id}}
    {{else}}
      No ID provided.
    {{/if}}
    <button type="submit">Remove Game</button>
</ul>

{{/each}}
